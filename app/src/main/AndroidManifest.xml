<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.ecommerce">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ECommerce">
        <activity android:name=".activites.PaymentActivity"></activity>
        <activity android:name=".activites.AddAddressActivity" />
        <activity android:name=".activites.AddressActivity" />
        <activity android:name=".activites.CartActivity" />
        <activity android:name=".activites.ShowAllActivity" />
        <activity android:name=".activites.DetailedActivity" />
        <activity android:name=".activites.OnBoardingActivity" />
        <activity android:name=".activites.MainActivity" />
        <activity android:name=".activites.RegistrationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activites.LoginActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <meta-data
            android:name="com.razorpay.ApiKey"
            android:value="rzp_test_2aHjDF9Grl6aX9"/>

    </application>

</manifest>